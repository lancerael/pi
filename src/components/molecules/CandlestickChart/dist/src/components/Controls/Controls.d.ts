import { ControlsProps } from './Controls.types';
export declare const Controls: ({ zoomLevel, setZoomLevel, setPanLevel, multiplier, visibleRange, length, }: ControlsProps) => JSX.Element;
export default Controls;
