/// <reference types="react" />
import { ButtonProps } from './Button.types';
export declare const Button: ({ children, status, dataSelector, ...buttonProps }: ButtonProps) => JSX.Element;
export default Button;
