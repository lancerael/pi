/**
 * The props for the styled tooltip container
 */
export interface StyledTooltipProps {
  /**
   * Is the tooltip visible?
   */
  isVisible: boolean
  /**
   * Is the tooltip rendered?
   */
  isRendered: boolean
}
