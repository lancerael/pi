name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      - name: Install pnpm
        uses: ./.github/workflows/install.yml

      - name: Test
        run: pnpm -r test
